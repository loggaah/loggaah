"use strict";

describe('FileAppender', () => {
    describe('#append()', () => {
        it('should save log messages to a file', () => {

        });

        it('should roll over files by size', () => {

        });

        it('should roll over files by time pattern', () => {

        });
    });

    describe('#getFilename()', () => {
        it('should return the current filename based on a time pattern', () => {

        });
    });
});